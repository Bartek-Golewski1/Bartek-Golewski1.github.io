{% extends 'base.html.twig' %}

{% block title %}Edit Song{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit Song</h1>

        {{ include('song/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-link mt-3" href="{{ path('app_song_index') }}">back to list</a>

        {{ include('song/_delete_form.html.twig') }}
    </div>
{% endblock %}
